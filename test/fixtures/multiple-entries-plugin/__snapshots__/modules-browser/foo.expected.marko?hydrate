if (window.$mwp) {
        __webpack_require__.p = $mwp;
      }

      __webpack_require__(/*! ./foo.marko?dependencies */ "./test/fixtures/multiple-entries-plugin/foo.marko?dependencies");
      window.$initComponents && window.$initComponents();